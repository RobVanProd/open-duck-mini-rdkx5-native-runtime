# Requirements Traceability

| Requirement | Implementation | Offline evidence | Hardware status |
| --- | --- | --- | --- |
| 101 observations / 14 actions | `contract.py`, `policy.py`, snapshot extractor/verifier; explicit default-off T247 selector | independent legacy-formula, named-mismatch tests, corrected-knee board snapshot; T247 115-D two-stage ABI and all asset identities exact | deployed v1 golden vector exact and unchanged; T247 uses only its separately reviewed 115-D opt-in path |
| 50 Hz / bounded timing | `AbsoluteTicker`, `TimingSeries`, probe; self-describing complete-sweep population and schema-bound provenance | mock summary, population-definition, comparison, collision/tamper tests | Gate 2 home hold tick p99/p99.9 20.002683/20.008892 ms and sweep max 4.721847 ms pass |
| Gate 1 single-servo echo/read | torque-off `probe_single_servo`; raw hash/auth/cutoff/framing summary | mock tick+summary schema and provenance tests | 10,000-read serial run `PASS_REVIEWED` |
| `duck_config.json` semantics | `config.py` | config, strict boolean, finite phase, and offset-order tests | live file validated and hashed; no writes performed |
| Frozen servo map | `constants.py` | logical map plus distinct wire-order test | logical map unchanged; ID 13 last removes reproduced grouped-read CRC |
| Script parity | `open_duck_x5.tools` and root wrappers | all four tools run on mock | no motor commands run |
| Crash/exit torque-off | `TorqueGuard`; cutoff-first cleanup; terminal cutoff status; Gate 2 launcher signals child before governor restoration | injected-crash, signal/hard-overrun during home, cleanup-order, failed-cutoff, runner and summary-validator tests | Gate 2 final cutoff `ok`; independent physical cutoff latency still pending |
| Direct STS3215 bus | `bus/sts3215.py`; transport validity and raw device status are separate | fixed frames, alarm-bearing payload, and fake-transport tests | all-14 moving home hold `PASS_REVIEWED` with 160,000/160,000 responses |
| SyncWrite + grouped read | preallocated bus frames; ID-routed wire order ending 14,13; exact 140-byte receive-before-parse collector | one-byte fragmentation, exact request order, parse-once, missing/CRC/partial/unexpected and ID-routing tests | Gate 2 home hold passes 10,000/10,000 with zero failures and 4.721847 ms sweep max |
| Timeout/CRC/partial taxonomy plus device alarms | `ErrorCode`, `ServoSnapshot.device_status`, JSONL and summaries | alarm-bearing payload remains fresh while alarm gate fails; per-class count tests | Gate 2 logged 160,000 ok and zero timeout/CRC/partial/device failures |
| Explicit staleness | `ServoSnapshot`, assembler rejection | stale-source tests; valid device-alarm payload is not mislabeled stale | Gate 2 sustained 10,000 ticks with zero stale servo samples |
| Serial minimum latency | verification/install scripts | shell syntax check | direct `/dev/ttyS1` UART 10,000-tick A/B complete; no material improvement over USB, so adapter-latency remedy is closed |
| Round-robin current/voltage/temp | extended read every tick modulo 14 with raw device status | register decode and alarm-telemetry tests | Gate 2 covers every servo 714/715 times; 6.9-8.4 V, 29-42 C, 0-0.182 A, zero alarms |
| SCHED_FIFO + isolated core | pre-spawn housekeeping partition plus verified control-thread isolation in `realtime.py`; exact-list and scheduler preflight | partition/offender/service-mask/parser tests | CPU 7 isolation, housekeeping 0-6, `SCHED_FIFO 80`, and one-variable `performance` governor benefit verified |
| Preallocated hot-loop data | arrays, packet frames, telemetry record pools | lint/tests; no JSON I/O in loop | allocation/timing profile pending |
| Complete evidence stream | bounded writers fail hard on pool/queue overflow or file errors | synchronous-open and forced-exhaustion tests | both Gate 2 stages have 10,000 contiguous records and zero drops |
| Gate 5 provenance and summary | hashed runtime-start record, strict tick/event schemas, continuity-checking `summarize_control_run`; distinct T247 contract/stage/route validation; separately recorded one-shot startup readiness | active/paused, command, envelope, schema, gap, path, exact T247 assets, ordered 250+600 duration, controller-before-serial, bounded anomaly routing, JSON-native two-number readiness phase, readiness fail-closed behavior, and default-v1 compatibility tests | replacement x=0 attempt halted before readiness record or active policy tick on a phase-vector serialization defect; all-14 cutoff independently verified; repaired retry requires a new freeze and authorization |
| Probe-decided Rust escalation | D002 and runbook | mock explicitly non-authoritative | RT tick gate green; Rust not triggered by bus/USB latency failure |
| BNO055 + contacts | transactional guarded `calibrate_imu`; independent capture re-verifier; bounded initial-publication barrier; fail-closed nine-label launcher; guarded labeled `probe_sensors`; chip-ID and exact calibration readback; per-label and matrix validators | capture authorization/timeout/atomicity/schema/readback tests; ready-timeout/no-output/pre-servo tests; restricted conversion, full status re-derivation, runner sequencing, raw-unit, axis-remap, polarity, freshness, provenance, tamper, and guard tests | `PASS_REVIEWED`: physical calibration exact; all 2,250 matrix rows fresh with zero worker errors; upright/paired tilts unambiguous; four dedicated contact populations exact; no servo path |
| Common timestamp clock | `clock.py` imported by all producers | coarse-clock issue caught by probe | X5 monotonic clock reports 1 ns resolution |
| ONNX warm-up | `OnnxPolicy` plus the T247 command-route host use single-thread sessions, bound float32 inputs, finite guards, and pre-loop warm-up | fake-runtime session/binding/warm-up/failure tests; exact T247 route and real-asset host checks | x=0 and x=.08 T247 routes pass X5 compute reserve at p99 0.786/1.606 ms; serial integration `NOT_RUN` |
| 3.75 rad/s telemetry monitor | `ActionPipeline`, control JSONL | envelope test | suspended replay pending |
| Watchdog >40 ms / consecutive bus faults | `Watchdog` in probe/runtime home entry and steady-state loops | work/period/failure plus injected home-overrun cutoff tests | Gate 2 home entry and hold completed with final cutoff `ok`; trip latency pending |
| Finite active replay duration | separate hard total-tick and valid-policy-tick caps; T247 serial guard requires exactly 850 active ticks | target-met/not-reached plus pre-I/O rejection of every non-850 T247 serial duration | all three x=0 attempts stopped at 0/850 active ticks; no duration claim and no advancement |
| Xbox/F710 controller parity | locked seven-command publication in `controller.py` | axis, A-edge pause, Y-edge head mode, LB sprint-factor, direct-Linux errno and bounded-work tests | original Xbox identity mapping verified; direct controller completed 10,000 ticks with one A edge and zero disconnects |
| Staged authority boundary | dual runtime assertions plus Gate-5-specific launcher assertion, finite exact-command replay, hash-frozen single-arm launcher, runbook, hold records | pre-I/O source/config/asset freeze, stage-order, x=.08 prior-review requirement, no-auto-advance, governor cleanup, and summary-validator tests | Gates 1-4 `PASS_REVIEWED`; all three x=0 attempts halted pre-policy and cannot be reused; grounded clearance is false |
| Board evidence extraction | safe-default collector, schemas, policy handoff | archive/manifest, secret-skip, 115-D rejection, and guard tests | Gate 1 bundle and 199-entry manifest verified |
| Variable assembly without manual COM measurement | guarded `configuration_collector.py`, strict raw trace fitter, automatic-profile/policy-envelope validator, two-stage launcher hard-blocked on the pending envelope identity, no-write `policy_envelope_closure.py`, and Git-object `policy_envelope_provenance.py`; backend provenance, trace/metadata/exact-config/pre-observation-envelope SHA binding, profile reproduction, timing gates, frozen hardware inventory; no policy-input mutation | complete 2,814-tick mock chain plus exact-fit, gap/stale/sensor-time/timing/bus/cross-joint/rate/current/manual-input/tampered-profile/post-collection-envelope-swap/authority/cutoff/out-of-envelope/domain-width/full-chain CLI and launcher ordering/scope/cleanup tests; independent envelope-hash, selected-ONNX, launcher-template, no-write candidate-hash, wrong-origin, uncommitted/wrong-artifact, and unrelated-history provenance tests | collector, locked launcher, provenance verifier, and closure checker offline verified; physical automatic profile run `NOT_RUN`; current policy held for configuration sensitivity |
| T247 offline completion and X5 host closure | policy completion audits plus exact context/command-route derivation, reserved X5 screen, opt-in production transaction, and independent summary | 20/20 conditions, 320/320 cells; 35/35 X5 route checks; 18/18 host-wiring checks; pause and failed-write safety paths; legacy-v1 compatibility | policy compute and host integration remain green; replacement x=0 halted before policy on telemetry serialization; repaired retry not yet frozen or authorized |
| Artifact hashing | `tools/hash_artifacts.py` | checked-in SHA-256 manifest | update per authorized gate |

No row marked pending may be treated as passed based on the mock run.
