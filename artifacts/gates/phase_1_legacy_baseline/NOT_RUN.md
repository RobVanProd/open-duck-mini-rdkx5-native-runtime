# NOT RUN — Legacy Hardware Baseline

Requires explicit authorization. Pre-registered comparison values from prior evidence are:

- `x=0.0`: 1.07% read errors.
- `x=0.08`: 2.68% read errors.
- maximum observed tick: 26 ms with read bursts.

These are inputs to the new comparison, not a replacement for the Phase 1 per-tick timing artifact.
